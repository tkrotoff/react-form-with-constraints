{
  "extends": "../../tsconfig",

  // List everything so that vscode displays potential errors for everything

  "files": [
    "src/index.ts"
  ],

  "include": [
    "src/*.test.ts",
    "src/*.test.tsx"
  ]
}
